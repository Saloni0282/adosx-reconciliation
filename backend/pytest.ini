[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
# Run with: python -m pytest reconciliation/tests/ -v
# (use python -m pytest to ensure the correct Python environment is used)
addopts = -v
